generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

/// Category for events (deity-based)
/// Definition source of truth: src/config/categories.ts
model Category {
  id              String          @id @default(cuid())
  name            String          @unique
  displayName     String
  icon            String
  color           String
  description     String?
  sortOrder       Int             @default(0)
  createdAt       DateTime        @default(now())
  updatedAt       DateTime        @updatedAt
  colorDark       String?
  eventCategories EventCategory[]

  @@index([sortOrder])
  @@map("categories")
}

/// Master event definition
model Event {
  id                  String             @id @default(cuid())
  name                String
  description         String?
  eventType           EventType          @default(OTHER)
  recurrenceType      RecurrenceType     @default(NONE)
  tithi               Tithi?
  nakshatra           Nakshatra?
  maas                Maas?
  tags                String[]           @default([])
  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt
  includeAdhika       Boolean            @default(false)
  isAdhikaOnly        Boolean            @default(false)
  sankranti           Sankranti?
  autoGenerated       Boolean            @default(false)
  namingKey           String?            @unique
  ruleConfig          Json?
  ruleType            RuleType?
  aliases             String[]           @default([])
  startTime           DateTime?          @db.Time(6)
  endTime             DateTime?          @db.Time(6)
  timingType          TimingType?
  categories          EventCategory[]
  occurrences         EventOccurrence[]
  seriesChildEntries  EventSeriesEntry[] @relation("SeriesChild")
  seriesParentEntries EventSeriesEntry[] @relation("SeriesParent")

  @@index([eventType])
  @@index([recurrenceType])
  @@index([tags], type: Gin)
  @@map("events")
}

/// Join table for Event ↔ Category many-to-many.
/// sortOrder=0 is the primary category (used for color/icon on calendar).
model EventCategory {
  id         String   @id @default(cuid())
  eventId    String
  categoryId String
  sortOrder  Int      @default(0)
  createdAt  DateTime @default(now())
  category   Category @relation(fields: [categoryId], references: [id])
  event      Event    @relation(fields: [eventId], references: [id], onDelete: Cascade)

  @@unique([eventId, categoryId])
  @@index([eventId, sortOrder])
  @@index([categoryId])
  @@map("event_categories")
}

/// Junction table for many-to-many parent-child event series relationships.
/// Allows one event (e.g. Maa Shailputri) to be a child of multiple parent series
/// (e.g. both Chaitra Navratri and Sharad Navratri).
model EventSeriesEntry {
  id            String   @id @default(cuid())
  parentEventId String
  childEventId  String
  dayNumber     Int?
  sortOrder     Int      @default(0)
  createdAt     DateTime @default(now())
  child         Event    @relation("SeriesChild", fields: [childEventId], references: [id], onDelete: Cascade)
  parent        Event    @relation("SeriesParent", fields: [parentEventId], references: [id], onDelete: Cascade)

  @@unique([parentEventId, childEventId])
  @@index([parentEventId])
  @@index([childEventId])
  @@map("event_series_entries")
}

/// Specific occurrence of an event on a date
model EventOccurrence {
  id        String    @id @default(cuid())
  eventId   String
  date      DateTime  @db.Date
  endDate   DateTime? @db.Date
  startTime DateTime? @db.Time(6)
  endTime   DateTime? @db.Time(6)
  notes     String?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  event     Event     @relation(fields: [eventId], references: [id], onDelete: Cascade)

  @@unique([eventId, date])
  @@index([date])
  @@index([endDate])
  @@map("event_occurrences")
}

/// Daily astronomical/lunar information
/// This is the source of truth for lunar data per day
model DailyInfo {
  id                   String         @id @default(cuid())
  date                 DateTime       @unique @db.Date
  locationName         String
  locationLat          Float
  locationLon          Float
  sunrise              DateTime?      @db.Time(6)
  sunset               DateTime?      @db.Time(6)
  moonrise             DateTime?      @db.Time(6)
  moonset              DateTime?      @db.Time(6)
  moonPhasePercent     Int            @default(0)
  moonPhaseType        MoonPhaseType?
  isWaxing             Boolean        @default(true)
  tithi                Tithi?
  nakshatra            Nakshatra?
  maas                 Maas?
  paksha               Paksha?
  createdAt            DateTime       @default(now())
  updatedAt            DateTime       @updatedAt
  karanaEndTime        DateTime?      @db.Time(6)
  karanaName           String?
  karanaType           String?
  nakshatraEndTime     DateTime?      @db.Time(6)
  tithiEndTime         DateTime?      @db.Time(6)
  yogaEndTime          DateTime?      @db.Time(6)
  yogaName             String?
  currentRashi         String?
  daysSinceSankranti   Int?
  lastSankrantiDate    DateTime?      @db.Date
  lunarDay             Int?
  maasType             String?
  moonSignNumber       Int?
  moonSignUpto         DateTime?      @db.Time(6)
  nextKaranaEndTime    DateTime?      @db.Time(6)
  nextKaranaName       String?
  nextKaranaNumber     Int?
  nextKaranaType       String?
  nextNakshatraEndTime DateTime?      @db.Time(6)
  nextNakshatraName    String?
  nextNakshatraNumber  Int?
  nextNakshatraPada    Int?
  nextTithiEndTime     DateTime?      @db.Time(6)
  nextTithiName        String?
  nextTithiNumber      Int?
  nextTithiPaksha      String?
  nextYogaEndTime      DateTime?      @db.Time(6)
  nextYogaName         String?
  nextYogaNumber       Int?
  samvatsaraName       String?
  samvatsaraNumber     Int?
  shakaSamvatName      String?
  shakaSamvatYear      Int?
  sunSignNumber        Int?
  sunSignUpto          DateTime?      @db.Time(6)
  vikramaSamvatName    String?
  vikramaSamvatYear    Int?
  isAdhika             Boolean        @default(false)
  sankranti            Sankranti?
  sankrantiTime        DateTime?      @db.Time(6)
  moonriseUtcIso       String?
  moonsetUtcIso        String?

  @@index([tithi])
  @@index([sankranti])
  @@index([sankranti, date])
  @@map("daily_info")
}

/// User preferences (single-user for now)
model UserPreference {
  id                     String       @id @default("default")
  currentTheme           String       @default("spiritual-minimal")
  defaultView            CalendarView @default(month)
  visibleEventTypes      EventType[]  @default([])
  visibleCategories      String[]     @default([])
  notificationsEnabled   Boolean      @default(false)
  notificationDaysBefore Int          @default(1)
  createdAt              DateTime     @default(now())
  updatedAt              DateTime     @updatedAt

  @@map("user_preferences")
}

model SadhanaPractice {
  id           String               @id @default(cuid())
  name         String
  type         SadhanaPracticeType
  notes        String?
  active       Boolean              @default(true)
  createdAt    DateTime             @default(now())
  mantraText   String?
  countSize    Int?
  updatedAt    DateTime             @updatedAt
  routineItems SadhanaRoutineItem[]
  items        SadhanaSessionItem[]
  goals        SadhanaGoal[]        @relation("SadhanaGoalToSadhanaPractice")

  @@map("sadhana_practices")
}

model SadhanaSession {
  id              String               @id @default(cuid())
  date            DateTime             @db.Date
  startedAt       DateTime?
  durationMinutes Int?
  notes           String?
  createdAt       DateTime             @default(now())
  updatedAt       DateTime             @updatedAt
  items           SadhanaSessionItem[]

  @@index([date])
  @@map("sadhana_sessions")
}

model SadhanaSessionItem {
  id              String          @id @default(cuid())
  sessionId       String
  practiceId      String
  quantity        Int
  unit            SadhanaItemUnit @default(malas)
  durationMinutes Int?
  notes           String?
  createdAt       DateTime        @default(now())
  updatedAt       DateTime        @updatedAt
  practice        SadhanaPractice @relation(fields: [practiceId], references: [id])
  session         SadhanaSession  @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@index([sessionId])
  @@index([practiceId])
  @@map("sadhana_session_items")
}

model SadhanaGoal {
  id            String            @id @default(cuid())
  type          SadhanaGoalType
  targetMalas   Int
  targetMinutes Int?
  active        Boolean           @default(true)
  createdAt     DateTime          @default(now())
  name          String?
  updatedAt     DateTime          @updatedAt
  practices     SadhanaPractice[] @relation("SadhanaGoalToSadhanaPractice")

  @@map("sadhana_goals")
}

model SadhanaRoutine {
  id        String               @id @default(cuid())
  name      String
  active    Boolean              @default(true)
  sortOrder Int                  @default(0)
  createdAt DateTime             @default(now())
  updatedAt DateTime             @updatedAt
  items     SadhanaRoutineItem[]

  @@map("sadhana_routines")
}

model SadhanaRoutineItem {
  id         String          @id @default(cuid())
  routineId  String
  practiceId String
  quantity   Int
  unit       SadhanaItemUnit @default(malas)
  sortOrder  Int             @default(0)
  createdAt  DateTime        @default(now())
  updatedAt  DateTime        @updatedAt
  practice   SadhanaPractice @relation(fields: [practiceId], references: [id])
  routine    SadhanaRoutine  @relation(fields: [routineId], references: [id], onDelete: Cascade)

  @@index([routineId])
  @@index([practiceId])
  @@map("sadhana_routine_items")
}

/// How to derive the observation time window for an event occurrence.
/// Times are calculated from DailyInfo sunrise/sunset for that specific date.
enum TimingType {
  /// Midpoint of the night: sunset + (nextSunrise - sunset) / 2 ± 30min
  NISHITA_KAAL
  /// Twilight window: sunset - 90min to sunset + 45min
  PRADOSH_KAAL
  /// Sunrise-based: sunrise to sunrise + 2h
  SUNRISE
  /// Sunset-based: sunset - 30min to sunset + 1h
  SUNSET
  /// Midday window: 2nd/5th to 3rd/5th of the day (Ganesh Chaturthi puja)
  MADHYAHNA
}

enum EventType {
  FESTIVAL
  PUJA
  VRAT
  JAYANTI
  TITHI
  SANKRANTI
  ECLIPSE
  OTHER
}

enum RecurrenceType {
  NONE
  YEARLY_LUNAR
  YEARLY_SOLAR
  MONTHLY_LUNAR
  MONTHLY_SOLAR
}

/// Event rule type for auto-generation from naming catalog
enum RuleType {
  TITHI
  SOLAR
  NAKSHATRA
  TITHI_NAKSHATRA
  WEEKDAY_TITHI
  PRADOSH
}

enum CalendarView {
  month
  week
  day
  agenda
}

/// Lunar fortnight (half of lunar month)
enum Paksha {
  SHUKLA
  KRISHNA
}

enum Tithi {
  PRATIPADA_SHUKLA
  DWITIYA_SHUKLA
  TRITIYA_SHUKLA
  CHATURTHI_SHUKLA
  PANCHAMI_SHUKLA
  SHASHTHI_SHUKLA
  SAPTAMI_SHUKLA
  ASHTAMI_SHUKLA
  NAVAMI_SHUKLA
  DASHAMI_SHUKLA
  EKADASHI_SHUKLA
  DWADASHI_SHUKLA
  TRAYODASHI_SHUKLA
  CHATURDASHI_SHUKLA
  PURNIMA
  PRATIPADA_KRISHNA
  DWITIYA_KRISHNA
  TRITIYA_KRISHNA
  CHATURTHI_KRISHNA
  PANCHAMI_KRISHNA
  SHASHTHI_KRISHNA
  SAPTAMI_KRISHNA
  ASHTAMI_KRISHNA
  NAVAMI_KRISHNA
  DASHAMI_KRISHNA
  EKADASHI_KRISHNA
  DWADASHI_KRISHNA
  TRAYODASHI_KRISHNA
  CHATURDASHI_KRISHNA
  AMAVASYA
}

enum Nakshatra {
  ASHWINI
  BHARANI
  KRITTIKA
  ROHINI
  MRIGASHIRA
  ARDRA
  PUNARVASU
  PUSHYA
  ASHLESHA
  MAGHA
  PURVA_PHALGUNI
  UTTARA_PHALGUNI
  HASTA
  CHITRA
  SWATI
  VISHAKHA
  ANURADHA
  JYESHTHA
  MULA
  PURVA_ASHADHA
  UTTARA_ASHADHA
  SHRAVANA
  DHANISHTA
  SHATABHISHA
  PURVA_BHADRAPADA
  UTTARA_BHADRAPADA
  REVATI
}

enum Maas {
  CHAITRA
  VAISHAKHA
  JYESHTHA
  ASHADHA
  SHRAVANA
  BHADRAPADA
  ASHWIN
  KARTIK
  MARGASHIRSHA
  PAUSHA
  MAGHA
  PHALGUNA
}

enum MoonPhaseType {
  NEW_MOON
  WAXING_CRESCENT
  FIRST_QUARTER
  WAXING_GIBBOUS
  FULL_MOON
  WANING_GIBBOUS
  LAST_QUARTER
  WANING_CRESCENT
}

enum SadhanaPracticeType {
  mantra_japa
  parayana
  other
}

enum SadhanaItemUnit {
  malas
  count
}

enum SadhanaGoalType {
  daily
  weekly
  lifetime
}

/// Solar transitions between zodiac signs (Sankranti)
enum Sankranti {
  MESHA_SANKRANTI
  VRISHABHA_SANKRANTI
  MITHUNA_SANKRANTI
  KARKA_SANKRANTI
  SIMHA_SANKRANTI
  KANYA_SANKRANTI
  TULA_SANKRANTI
  VRISHCHIKA_SANKRANTI
  DHANU_SANKRANTI
  MAKARA_SANKRANTI
  KUMBHA_SANKRANTI
  MEENA_SANKRANTI
}
