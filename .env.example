# =============================================================================
# DHARMA CALENDAR - Environment Variables
# =============================================================================
# Copy this file to `.env` and fill in your values.
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Required)
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
#
# Local development:
DATABASE_URL="postgresql://postgres:password@localhost:5432/dharma_calendar?schema=public"
#
# Docker (uses internal network, 'db' is the service name):
# DATABASE_URL="postgresql://dharma:dharma_secret_2025@db:5432/dharma_calendar?schema=public"

# -----------------------------------------------------------------------------
# DOCKER COMPOSE (Optional - have sensible defaults)
# -----------------------------------------------------------------------------
# These are used by docker-compose.yml
# Only set these if you want to override the defaults

# PostgreSQL credentials (defaults shown)
# POSTGRES_USER="dharma"
# POSTGRES_PASSWORD="dharma_secret_2025"
# POSTGRES_DB="dharma_calendar"

# Port mappings (defaults shown)
# DB_PORT="5432"
# APP_PORT="3000"

# -----------------------------------------------------------------------------
# APPLICATION (Optional)
# -----------------------------------------------------------------------------
# Node environment (development, production, test)
# NODE_ENV="development"
