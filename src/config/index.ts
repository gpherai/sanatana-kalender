/**
 * Config Index
 * Re-exports all config for clean imports
 *
 * Usage:
 *   import { THEME_CATALOG, DEFAULT_THEME_NAME } from '@/config'
 *   import { CATEGORY_CATALOG, toCategoryOption } from '@/config'
 */

// Theme config
export * from "./themes";

// Category config
export * from "./categories";
